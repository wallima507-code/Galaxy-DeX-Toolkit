<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <queries>
        <package android:name="com.android.settings" />
        <package android:name="com.android.systemui" />
        <package android:name="com.samsung.android.smartmirroring" />
        <intent><action android:name="android.settings.CAST_SETTINGS" /></intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Galaxy DeX Toolkit"
        android:supportsRtl="true"
        android:theme="@style/Theme.A56Toolkit">
        <activity android:name=".DexScannerActivity" />
        <activity android:name=".DexTouchpadActivity" />
        <activity android:name=".InputDevicesActivity" />
        <activity android:name=".WallpaperActivity" />
        <activity android:name=".ActivityExplorerActivity" />
        <activity android:name=".HiddenSettingsActivity" />
        <activity android:name=".DeviceInfoActivity" />
        <activity android:name=".DesktopToolsActivity" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
